---
title: "Chemotherapy Dose (BSA-based)"
description: "Compute an individual chemotherapy drug dose from a protocol-specified mg/m² dose and the patient's body surface area: calculated_dose = dose_per_m2 × BSA, then final_dose after any percentage reduction. BSA is not capped unless the caller explicitly enables the protocol-specific 2.0 m² cap."
sidebar:
  label: "Chemotherapy Dose (BSA-based)"
search:
  tags: ["chemo_dose_bsa", "formula"]
---

{/* GENERATED from repository-root OathMCP sources. Edit scripts/generate-calculators.mjs, not this page. */}

<Badge variant="success">scenario_verified</Badge> <Badge>formula</Badge> <Badge>Spec 1.0.0</Badge>

:::warning
This calculator provides decision support. Confirm that its declared population,
setting, model, timing, inputs, units, and limitations apply. A qualified
clinician remains responsible for interpretation and every clinical decision.
:::

## Clinical use

| Dimension | Declared contract |
| --- | --- |
| Population | Compute an individual chemotherapy drug dose from a protocol-specified mg/m² dose and the patient's body surface area: calculated_dose = dose_per_m2 × BSA, then final_dose after any percentage reduction. BSA is not capped unless the caller explicitly enables the protocol-specific 2.0 m² cap. |
| Setting | Clinical decision support; apply only in the context declared by this calculator. |
| Model | `chemo_dose_bsa` · 1.0.0 |
| Model kind | formula |
| Variant | Not separately declared |
| Review date | 2026-07-16 |
| Review after | Not separately declared |

### Exclusions and limitations

**When not to use:** Verify the regimen, body-size dosing policy, dose-reduction rationale, and final rounded dose with oncology pharmacy. Do not enable the BSA cap unless the applicable protocol explicitly requires it.

- Verify the regimen, body-size dosing policy, dose-reduction rationale, and final rounded dose with oncology pharmacy. Do not enable the BSA cap unless the applicable protocol explicitly requires it.

## Inputs

| Field | Type | Required | Default | Plausible | Hard limits | Description |
| --- | --- | --- | --- | --- | --- | --- |
| `dose_per_m2` | number | Yes | — | 1–3000 | 0.1–10000 | Protocol-specified dose in mg/m². |
| `bsa` | number | Yes | — | — | 0.5–3 | Patient body surface area in m². |
| `dose_reduction` | integer | No | `0` | — | 0–100 | Percentage dose reduction (0 for full dose, e.g. 25 for a 25% reduction). |
| `cap_bsa` | boolean | No | `false` | — | — | Set true only when the applicable regimen explicitly caps BSA at 2.0 m². |

For `quantity` fields, submit an object with explicit `value` and `unit` fields.
Hard limits are operational safety envelopes, not clinical cutoffs. Values
outside plausible ranges may calculate with a warning when still within hard limits.



## Outputs

| Field | Type | Availability | Primary | Description |
| --- | --- | --- | --- | --- |
| `calculated_dose` | number · mg | Always | No | Dose before any reduction, in mg, rounded to 1 decimal place. |
| `final_dose` | number · mg | Always | Yes | Dose after any percentage reduction, in mg, rounded to 1 decimal place. |
| `bsa_used` | number · m² | Always | No | The BSA value used after any explicitly requested cap, rounded to 2 decimal places. |
| `bsa_was_capped` | boolean | Always | No | Whether the BSA was capped at 2.0 m². |
| `reduction_applied` | number · % | Always | No | The percentage dose reduction applied. |

## Interpretation bands

No deterministic interpretation bands are declared for this calculator.

## Warnings, constraints, and adjustments

- **bsa when &gt;2.0; where cap_bsa ==true:** BSA above 2.0 m² was capped at 2.0 m² for dosing.
- **Adjustment `bsa_cap_2`:** cap at 2 when its declared condition applies.

## MCP request example

The example is drawn from the source-derived validation dossier when available.
It demonstrates request shape; it is not a recommendation to use this calculator
for a particular person.

```json title="calculate"
{
  "id": "chemo_dose_bsa",
  "inputs": {
    "dose_per_m2": 100,
    "bsa": 2.5,
    "dose_reduction": 0,
    "cap_bsa": true
  }
}
```

Before first use, call `describe_calculator` with the canonical ID `chemo_dose_bsa`
and compare the returned contract to this page.

## Implementation assurance

| State | Claims | Executable claims | Required scenarios | Blockers |
| --- | ---: | ---: | ---: | --- |
| `scenario_verified` | 20/20 | 19/19 | 10/10 | None recorded |

This is repository-defined implementation assurance, not an unqualified claim
of clinical validation, regulatory approval, or suitability for an individual patient.

## Evidence

The MCP resource for this calculator is `calc://chemo_dose_bsa/evidence`.

| ID | Role | Citation and locator |
| --- | --- | --- |
| `source_1` | derivation | [Griggs JJ, Bohlke K, Balaban EP, et al. Appropriate Systemic Therapy Dosing for Obese Adult Patients With Cancer: ASCO Guideline Update. J Clin Oncol. 2021;39(18):2037-2048.](https://doi.org/10.1200/JCO.21.00471)<br />doi:10.1200/JCO.21.00471 |
| `source_2` | reference | [Kent and Medway Cancer Collaborative. Guidance on Capping of BSA for the Purposes of Calculating Cytotoxic Chemotherapy. Version 2.](https://www.kmcc.nhs.uk/s3/assets/guidance-on-capping-of-bsa-for-the-purposes-of-calculating-cytotoxic-chemotherapy-v2.pdf)<br />Version 2 policy, recommendation to cap at BSA 2.0 m² unless the clinician specifies otherwise |

<Panel title="Repository source">
  Calculator ID `chemo_dose_bsa`; spec version `1.0.0`; generated from
  the checked-in spec, validation dossier, and assurance state in this OathMCP
  repository. Use the evidence resource and repository history when evaluating
  source currency.
</Panel>
